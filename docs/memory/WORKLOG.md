# Worklog

Use concise high-value entries only.
This is not a changelog. Do not record routine releases, version bumps, or implementation summaries.

## Template

### YYYY-MM-DD - Summary

- why this is durable
- what future mistake it prevents
- evidence
- where future contributors should look

## Example

### 2026-03-15 - Pagination cursor must be opaque to clients

- **Why durable**: three features so far have tried to expose raw database offsets as pagination cursors, each time creating breaking changes when the underlying query changes
- **Future mistake prevented**: next time a feature adds pagination, the implementer will know to use opaque cursors from the start
- **Evidence**: specs 018, 024, and 031 all required pagination rework; see DECISIONS.md entry on API pagination
- **Where to look**: `src/api/pagination.ts`, `docs/memory/DECISIONS.md`

## Counter-Example (do not write entries like this)

> ### 2026-03-15 - Updated pagination
>
> - Changed pagination to use cursors
> - Deployed to staging

This is a changelog entry, not a durable lesson. It records what happened, not what was learned.

### 2026-08-19 - F069 Phase 9 — 13-step dynamic wizard evolution + 3 durable bug patterns + 1 architectural boundary

- **Why durable**: F069 shipped as a 5-step MVP; user feedback drove a rework into a dynamic 5-to-10-step flow across 13 total steps driven by 10 skip predicates (`skipCreate`, `skipAbilitiesGate`, `skipAbilities`, `skipEnable`, `skipProPromo`, `skipProActivate`, `skipConnectorsDetail`, `skipClient`, `skipNpm`, `skipWpcli`). Captures the reusable pattern for admin wizards where every step is conditional: `STEP_ORDER` + `shouldSkip(step, skips)` in the router + centralized skip predicates in the App orchestrator + auto-skip effect that forwards past skippable steps. The 3 new hooks (`useFooterAction`, `useHideContinue`, `useWizardAdvance`) formalize the "Back · Continue · <combined primary action>" footer pattern that appeared in Steps 3/5/6; any future setup wizard in this codebase should reuse them rather than reinventing.
- **Future mistake prevented**: (1) captures the "advance-on-click captures stale skips" React closure race so future picker steps use fresh-value-driven navigation instead of context advance mid-callback; (2) codifies the "full-screen loading overlay owned by StepLayout, driven by `busy = isLoading || footerAction?.isLoading`" pattern so per-step inline spinners don't diverge; (3) documents why the "initial-loading" gate must distinguish first-hydrate from refetch (B46); (4) documents the `set_transient` false-on-unchanged silent-failure class (B45); (5) documents why `.wordpress-org/` icon references 404 on hosts (B47); (6) codifies that this plugin's `is_enabled` layer is an admin safety layer on top of MCP Adapter, NOT derived from the framework (A21).
- **Evidence**: Branch `069-quick-setup-13-step-flow` off `069-mcp-quick-setup-wizard`. Commit `f3e62b3` (~35 files, +2810 / -1187). Backend `VALID_STEPS` bumped `[1..5] → [1..13]`; new `POST /install-plugin` route with slug whitelist for `acrossai-abilities-manager` + `acrossai-pro` via `Plugin_Upgrader`; `handle_step` piggybacks fresh `abilities` count into every /step response so `skipAbilities` fires without a follow-up /state fetch; `write_scratchpad` tolerates `set_transient()`'s no-change false return via read-back verification. Frontend: `STEP_ORDER` grew to 13; `stepVisibilityTable` centralizes step ↔ skip mapping; `hasHydratedOnceRef` prevents step unmount during refetch; server_id mirrored into URL as `?server=<id>` for shareable deep links; `visibilitychange` + `popstate` refetch listeners added alongside the existing `focus` fallback. All PHPCS clean; JS build clean; PHPUnit `test_post_step_succeeds_when_scratchpad_payload_unchanged` pins the B45 regression.
- **Where to look**: `specs/069-mcp-quick-setup-wizard/tasks.md` §Phase 9 (T064-T103, 40 concrete task IDs). `specs/069-mcp-quick-setup-wizard/plan.md` §Summary Post-MVP note. Router pattern: `src/js/quick-setup/hooks/useWizardRouter.js`. Skip-predicate composition: `src/js/quick-setup/App.jsx` (`skips`, `stepVisibilityTable`, auto-skip effect). Gate-card visual grammar: `qs__gate-card` + `qs__gate-card--warning` in `src/scss/quick-setup.scss`. Related durable memory added in the same push: B45, B46, B47, A21.

### 2026-08-09 - Gate-tied cache-exclusion warnings belong in `acrossai_notices`, not only inline in the settings section

- **Why durable**: an operator who enables a feature that depends on a nonce/OTP-carrying URL rarely revisits the section description afterward, so an inline-only warning is invisible on every subsequent visit and to every co-admin who did not perform the toggle. Publishing the same warning as a persistent row in the cross-plugin `acrossai_notices` collection surfaces it on the Notices submenu + WP-native dismissible summary until the operator explicitly dismisses it. The general rule: **any setting whose ON state introduces a URL that MUST NOT be page-cached MUST push a conditional notice into `acrossai_notices` gated on that setting** — do not rely on the setting-section banner alone. Mirrors the `acrossai_ai_connectors_page_cache_exclusions_required` notice on `WP_CACHE`; here we gate on the plugin's own `acrossai_mcp_npm_login_enabled` option instead of `WP_CACHE`.
- **Future mistake prevented**: next feature adding a setting that exposes a nonce-bearing or OTP-bearing URL (device-grant polling, PKCE authorize, embed-consent, magic-link flows) will push a conditional cache-exclusion notice via `Notices::register_shared_notices()` at the same time the setting ships. Prevents the "silently 401/302-loops in production behind a cache plugin" failure mode where nothing on the admin surface warns the operator until an end-user reports a broken flow.
- **Evidence**: PR #70 (`feat/cli-auth-cache-exclusion-notice`) — 15-line addition to `admin/Partials/Notices.php::register_shared_notices()`, gated on `get_option( 'acrossai_mcp_npm_login_enabled', false )`, uses `FrontendAuth::get_base_url()` for the URL, `id = acrossai_mcp_manager_cli_auth_cache_exclusion`. Mirrors the inline banner already present at `admin/Partials/SettingsMenu.php::render_npm_section_description()` (lines 195-207).
- **Where to look**: `admin/Partials/Notices.php:145-158` (reference impl). `admin/Partials/SettingsMenu.php:195-207` (matching inline banner — inline stays for operators mid-flow, `acrossai_notices` row catches everyone else). Related: the vendor `acrossai-ai-connectors` version at `admin/Partials/Notices.php` (id `acrossai_ai_connectors_page_cache_exclusions_required`, gated on `WP_CACHE` — the site-wide analog).

### 2026-07-31 - F040 — Cross-plugin subsystem migration via `class_exists()` self-disable probe (zero data migration)

- **Why durable**: F040 codified the pattern for splitting a bundled subsystem out into a companion plugin without any data migration. The mechanism is atomic: the parent stops shipping a canonical class (`AuthorizationController`), the companion's `class_exists()` self-disable probe flips from "sit inert" to "take over", and ownership transfers on the next request. **Zero table renames, zero row copies, zero downtime window.** Also codified: (1) when a subsystem gets its own plugin, prefer **code-only migration** (identical `$name`/`$version`/`$db_version_key` on both sides — BerlinDB `maybe_upgrade()` becomes a no-op) over data-migration; (2) interrogate assumed hard dependencies during `/speckit-clarify` — Q5 caught that adding `Requires Plugins: acrossai-ai-connectors` to mcp-manager would have broken every free-tier install because OAuth is one of three parallel connection methods (npm / clients / ai-connectors), not the only one; (3) YAGNI is a first-class /speckit-clarify outcome — Q4 dropped the `class_alias` compat shim and Q6 dropped the admin-notice safety net once we established there are no external users to defend, reducing the feature to pure deletions + a version bump with **zero new code**.
- **Future mistake prevented**: (a) Next feature splitting a bundled subsystem into an add-on will use the self-disable probe pattern instead of a data-migration script (which is what the pre-F040 draft assumed for months). (b) Next `/speckit-specify` invocation with an assumed hard dependency will get clarified before the spec locks — Q5's discovery of the three-connection-paths architecture is the model for interrogating "hard dep vs premium upsell" framing. (c) Next feature that reaches for a compat shim or admin-notice safety net will first ask whether the user population it protects actually exists — Q4 + Q6 are the templates.
- **Evidence**: 99-file git diff (~90 deletions of `includes/OAuth/**`, `includes/Connectors/**`, 4 `includes/Database/OAuth*` + `ConnectorApprovedUsers/` modules, `admin/Partials/ServerTabs/AIConnectorsTab.php`, `templates/oauth/consent.php`, `src/{js,scss}/ai-connectors.*`, `build/*/ai-connectors.*`, ~30 PHPUnit test files including `tests/phpunit/OAuth/**` and `tests/phpunit/Database/OAuth*`) plus 8 targeted edits (`Activator.php`, `Main.php`, `admin/Main.php`, `admin/Partials/ServerTabs/Registry.php`, `uninstall.php`, `webpack.config.js`, `public/Discovery/ConnectionMethodRegistry.php` per FR-019, `acrossai-mcp-manager.php` header 0.1.9→0.2.0). Companion `acrossai-ai-connectors` v0.5.0 audited across 44 combined invariant checks (23 structural + 21 differential wiring — see `specs/040-migrate-ai-connectors-to-companion/research.md`) all PASS. Pre-flight callers grep: 123 hits, all inside deletion-scope or covered by wiring edits + one out-of-scope hit surfaced (`public/Discovery/ConnectionMethodRegistry.php`, captured as FR-019). Post-flight grep: 0 hits.
- **Where future contributors should look**: `specs/040-migrate-ai-connectors-to-companion/{spec,plan,research,data-model,contracts,quickstart}.md` for the full feature dossier. `docs/planings-tasks/040-migrate-ai-connectors-to-companion.md` for the nav pointer. Companion's self-disable probe: `wp-content/plugins/acrossai-ai-connectors/includes/Main.php::mcp_manager_still_owns_oauth()` (called from 6 bootstrap paths). This WORKLOG entry's clarification-driven-YAGNI pattern (Q4/Q6 sequence): re-read `specs/040-.../spec.md` `## Clarifications` section for the reasoning chain.
- **Bugs surfaced by this feature (both in the companion, both hidden by the self-disable probe until F040 flipped it)**:
  - **B42 (2026-07-31)** — `add_rewrite_rule()` called on `plugins_loaded` fatals because `$wp_rewrite` is instantiated in wp-settings.php AFTER `do_action('plugins_loaded')`. Companion's `maybe_run_first_run_migration()` (hooked on `plugins_loaded @ 20`) called `OAuthRouter::instance()->register_rewrite_rules()` → `add_rewrite_rule()` → fatal. Fix: re-hooked to `init @ 999` + deleted the redundant `register_rewrite_rules()` call. Captured in `docs/memory/BUGS.md` §B42.
  - **B43 (2026-08-01)** — Loader signature mismatch: `$this->loader->add_filter('hook', array($this, 'method'), 10, 2)` copies the WP-native `add_filter($hook, $callback, ...)` shape into the plugin's Loader, whose signature is DIFFERENT (`$hook, $component, $callback_string, $priority, $accepted_args`). The Loader stored `component=[obj,'method']` (an array!) and `callback=10` (integer!), so at fire time WP_Hook's `call_user_func_array` fatalled with "first array member is not a valid class name or object". Surfaced when the site admin loaded a server-edit screen (Registry::visible_tabs → apply_filters → mis-registered callback). Fix: rewrote the call to use the 5-arg Loader shape. Captured in `docs/memory/BUGS.md` §B43.
- **Generalizable meta-lesson (PATTERN-PROBE-HIDES-LATENT-BUG)**: any long-lived defense-in-depth guard that short-circuits a code path can hide bugs in that path until the guard deactivates. Two bugs in two consecutive days (B42, B43) both surfaced in the companion the moment its self-disable probe flipped from "sit inert" to "take over". Neither bug was reachable while mcp-manager still owned OAuth. **When a probe/guard is being retired, treat probe-deactivation as its own release event that deserves smoke-testing** (activate the plugin, hit every major admin screen, load every REST route the probe-guarded code paths touch). One shakedown incident's worth of latent bugs surfacing is expected — the point of the smoke test is to surface them BEFORE production traffic does. Both bugs also share a "fatal at fire time, not at registration time" property that makes static review insufficient — runtime `_doing_it_wrong` guards at the abstraction boundaries would have caught B43 at wiring time and B42 at first-fire on the wrong hook.

### 2026-07-28 - F037 — Per-server Embeds tab + AbstractReactMountServerTab primitive shipped with 3 post-plan pivots

- **Why durable**: F037 established two governance patterns worth remembering — (a) how to shape a **reusable admin-UI primitive** for third-party companion plugins (`D38`); (b) how to **retrofit the spec** when post-`/speckit-implement` pivots outrun the pre-plan intent, via a Post-Plan Pivots section + new FRs + new contract files (this session's `/speckit-analyze` → Path A retrofit).
- **Future mistake prevented**: Next feature with a similar per-server admin UI (Abilities/Tools migration to the new base is queued via GitHub issue) will extend `AbstractReactMountServerTab` in ~40 LOC + declare identity + state methods, instead of hand-rolling enqueue + REST + noscript. Next feature that undergoes mid-implementation pivots will run `/speckit-analyze` post-implement + do the Path A retrofit, not leave the spec silently drifted.
- **Evidence**: Commits `8d55d21` (F037 core delivery — 54 files, 7526 LOC) + `0c8c5f8` (post-arch-review fixes — RT1 SC-006 amendment, RT2 per-listener isolation helper, RT3 find_server_row overridable). Three post-plan pivots documented in `specs/036-shortcode-block-embeds/spec.md#post-plan-pivots`: (A) per-DTO gate redesign — `is_enabled_for_server` widened to 3 args, observability action widened to 5 args (BREAKING); (B) meta-table storage refactor — junction table + `embeds_enabled` column BOTH retired in favor of WP-canonical `wp_acrossai_mcp_servers_meta` (see `A19`); (C) `AbstractReactMountServerTab` extraction — the reusable primitive owning asset enqueue + REST + storage contract + self-registration. Spec/plan/tasks/data-model/contracts all retrofitted; 8 rescoped-pending tasks queued.
- **Where future contributors should look**: `admin/Partials/ServerTabs/AbstractReactMountServerTab.php` for the reusable primitive with copy-pastable third-party consumer example in the class-level docblock; `includes/Database/MCPServerMeta/` for the WP-canonical meta table pattern (see `A19`); `includes/Embeds/AbstractEmbedTransport::fire_action_isolated()` for per-listener observability isolation (see `D39`). Related decisions born this session: `D37` (`DEC-ADMIN-UI-REACT-FIRST` — Q4 pivot away from hand-rolled forms + vanilla JS), `D38` (reusable-primitive `register()` exception to A1), `D39` (per-listener observability isolation), `A19` (WP-canonical meta table pattern).

### 2026-07-20 - F030 Per-server ability permission_callback override — 6-layer defensive gating framework + explicit-exposure-only carve-out + wp_json_encode-for-JS lesson generalized

- **Why durable**: F030 codified four reusable durable patterns that follow-on features should reach for by default: (1) `D29` (`DEC-F030-PERMISSION-CALLBACK-OPERATOR-OPT-IN-BYPASS`) — the six-layer defensive gating framework for any operator-visible `permission_callback` bypass (cap + nonce + persistent banner + confirm prompt + `CurrentServerHolder` scope + `ExposureResolver` junction gate); scoped carve-out from `D24`'s exposure-≠-authorization corollary. (2) `D30` (`DEC-F030-EXPLICIT-EXPOSURE-ONLY`) — F030 intentionally passes empty `$meta` to `ExposureResolver::resolve()` to collapse the `meta.mcp.public` fallback → operator-opt-in bypass ONLY applies to abilities with an explicit junction row; scoped carve-out from `DEC-ABILITY-OVERRIDE-RESOLUTION`. (3) `B35` — `wp_register_ability_args` filter-priority slot map (P10 CallbackReplacer, P100000 sibling `acrossai-abilities-manager`, P999999 F030) codified so any new consumer picks a non-conflicting slot AND reviewers can spot ordering regressions at PR time. (4) `B36` — inline `<script>` interpolation of dynamic PHP values into a JS string literal MUST use `wp_json_encode()`, NOT `esc_html()` or `esc_attr()` (both are HTML-context escapers that leave `'`/`"`/`</script>` un-escaped for JS parsers → stored XSS). Also codifies `DEV5` — per-server-edit tab sub-forms MAY use hand-rolled admin HTML instead of DataForm when ≤ 3 fields with no filter/sort/pagination requirement (Update Server + Danger Zone + F030 Access Control per D13 escalation threshold ≥ 2 features).
- **Future mistake prevented**: (a) A future feature reaches for "just set `permission_callback = true`" without the six-layer gating — `D29` is the audit checklist that MUST pass first; if any layer is absent, the feature does NOT ship (falls back under `D24`'s default rule). (b) A future maintainer "fixes" F030's empty-`$meta` call as if it were an oversight — the 20-line inline comment at `PermissionOverrideProcessor.php:123-142` + `D30` document the intent explicitly. (c) A future plugin registers a `wp_register_ability_args` filter at an arbitrary priority without checking the `B35` slot map — silently subsumes F030's semantics. (d) A future feature emits an inline `<script>` and reaches for `esc_html()` on the interpolated value out of habit — `B36` grep gate catches at PR time. (e) A future per-server-tab sub-form re-litigates the DataForm question — `DEV5` is the pre-approved carve-out.
- **Evidence**: ~1,200 net LoC across 8 modified files + 3 new source files + 5 PHPUnit test files + 4 spec/memory/planning/security-review docs. Implementation code: `includes/Abilities/PermissionOverrideProcessor.php` (170 lines, singleton + closure factory), `admin/Partials/ServerTabs/Partials/AbilitiesManagerPromoCard.php` (200 lines), `admin/Partials/ServerTabs/AccessControlTab.php` (+~450 lines with layout styles + form render + inline script), `admin/Partials/Settings.php` (+~110 lines: router branch + `handle_save_permission_override()`), `includes/Main.php` (+21 lines: 4-line Loader wiring + companion cache-clear hooks). DB: `includes/Database/MCPServer/{Schema,Table,Row}.php` (D28 3-part contract: `$version` 1.1.1 → 1.1.2 + `upgrade_to_1_1_2()` idempotent ALTER + property + int cast). Tests: 5 PHPUnit files, ~20 test methods covering closure fall-through matrix (4 branches) + save-handler nonce/cap rejection + isolation invariants (multi-server + non-MCP + P999999 vs P100000) + D28 upgrade regression + promo card render states. Quality gates: PHPCS clean on all F030 files (0 errors, 1 non-blocking `_reset_cache_for_tests` warning matching `ExposureResolver` precedent), PHPStan L8 zero errors, `mcpclients` suite 67 tests all pass, `validate-packages` clean. Security reviews: plan-phase (MODERATE — 1 M + 2 L + 2 I) → staged (INFORMATIONAL — all 5 findings closed/documented). **Bonus fix**: patched pre-existing test-infrastructure gap — added `abilities`, `database`, `mcp` PHPUnit suites in `phpunit.xml.dist` + 3 matching CI workflow steps. F011/F017/F026 legacy tests + all F030 new tests were previously orphaned (no suite covered them, CI never ran them); now covered.
- **Where to look**: `docs/planings-tasks/030-per-server-permission-override.md` for the spec + verification recipe. `specs/030-per-server-permission-override/{spec,plan,tasks,memory-synthesis}.md` for the Spec Kit artifacts. `includes/Abilities/PermissionOverrideProcessor.php:107-153` for the reference closure impl (all six layers wired) + the 20-line inline comment on the empty-`$meta` decision. `admin/Partials/ServerTabs/AccessControlTab.php:509-513` for the canonical `wp_json_encode`-for-JS pattern (B36 reference). `docs/security-reviews/2026-07-20-030-per-server-permission-override-plan.md` + `-staged.md` for the two-phase security posture. Related durable memory: `D29` (six-layer bypass framework), `D30` (empty-meta carve-out), `B35` (filter-priority slot map), `B36` (wp_json_encode for JS-context), `DEV5` (per-server-tab hand-rolled form exception), `D24` (the default rule D29 carves out), `DEC-ABILITY-OVERRIDE-RESOLUTION` (the canonical resolver D30 selectively narrows), `A17` (request-scoped REST context — F030 depends on it), `D28` (BerlinDB schema-drift reconciliation — F030's column-add follows it).

### 2026-07-18 - F029 OAuth /token accepts HTTP Basic auth + DCR-registered clients attributed to connector profiles + BerlinDB schema-drift reconciliation

- **Why durable**: F029 codified four reusable patterns that follow-on features should reach for by default: (1) `read_client_credentials_from_header()` — RFC 6749 §2.3.1 Basic auth parsing with CGI fallback + strict base64 decoding, applicable to any future OAuth grant added to `/token`; (2) attribution walk at data creation time (`ConnectorProfileRegistry::get_profiles()` + `matches_dcr_client()` first-match-wins), applicable to any future resource type that needs a semantic slug computed from raw shape at write time; (3) `client_secret_post` softening via PKCE fallback as `D27` — worth revisiting on every future OAuth grant or PKCE-mode change; (4) BerlinDB schema-drift reconciliation via `$upgrades` callbacks + `admin_init` trigger as `D28` — mandatory 3-part contract (bump `$version` + register `$upgrades` callback + wire admin_init) for every future Schema.php evolution on an existing table.
- **Future mistake prevented**: (a) Assuming OAuth clients always put credentials in the body — spec-conformant clients use Basic auth on the header; every future OAuth endpoint MUST handle both. (b) Adding a new write path for a resource without auditing every downstream admin gate keyed on its data fields (`B33` — F024's connector gate silently fell open for every DCR client for months before F029 caught it). (c) Hard-rejecting `client_secret_post` clients that don't submit a secret when PKCE is present — the softening pattern in `D27` is now the canonical response for real-world MCP host interop. (d) Bumping a BerlinDB Table `$version` without registering a matching `$upgrades` callback — BerlinDB silently stamps the new version and touches nothing, leaving Schema.php drift latent (`B34` — took down Claude OAuth on procureco.uk when `oauth_tokens` drifted; latent in `servers` + `cli_auth_logs` before F029).
- **Evidence**: PR #37 — 5 commits totalling `+400` net LoC across 8 files. OAuth code: `includes/OAuth/TokenController.php` +90/−19, `includes/OAuth/ClientRegistrationController.php` +14/−1. DB reconciliation: `includes/Database/CliAuthLog/Table.php` +73/−17 (`$upgrades` + `upgrade_to_1_0_1` ALTER MODIFY for 3 type-drifted columns), `includes/Database/MCPServer/Table.php` +81/−7 (`$upgrades` + `upgrade_to_1_1_1` ALTER ADD COLUMN for 3 F025 flags), `includes/Main.php` +46 (new `reconcile_database_schemas()` + Loader-wired `admin_init@3` hook). CI: 8/8 checks green across every commit (F021 gates + PHPCS + PHPStan L8 + PHPUnit integration on WP latest + PHPUnit pure + JS Lint + PHP 8.1+ compat + Package Hierarchy).
- **Where to look**: `docs/planings-tasks/029-oauth-token-basic-auth-and-dcr-attribution.md` for the spec + verification recipe. `specs/029-oauth-token-basic-auth-and-dcr-attribution/{spec,plan,tasks}.md` for the Spec Kit artifacts. `TokenController::read_client_credentials_from_header()` for the reusable header parser. `ClientRegistrationController::handle_register()` for the reusable profile-walk attribution pattern. `includes/Database/CliAuthLog/Table.php` + `MCPServer/Table.php` for reference `$upgrades` callback impls. `Main::reconcile_database_schemas()` for the admin_init trigger pattern. Related durable memory: `D27` (soft-auth via PKCE), `D28` (schema-drift reconciliation), `B33` (admin-gate silent-bypass), `B34` (silent write-loss on schema drift), `F027` (v0.1.2 DCR default — the OAuth source-of-truth complement).

### 2026-07-17 - F028 Retire Freemius integration; consume main-menu 0.0.22 filter-driven Add-ons page

- **Why durable**: A vendor package can shed a heavy dependency (`freemius/wordpress-sdk` here) in a minor bump, and the retirement pattern on the consumer side is symmetric across every AcrossAI plugin: (1) bump the pin, (2) delete the vendor-class instantiation block, (3) if the vendor's replacement is a filter, register a small `Admin\Partials\<X>Filter` singleton that adjusts the array for local context, (4) mark related DEC / BUG entries **Superseded (F###)** in `INDEX.md` + source files without deleting bodies (per PATTERN-MEMORY-SUPERSESSION-VS-ANNOTATION until an audit prunes them). F028 also codifies the **consumer self-exclusion filter pattern**: when a vendor renders a shared list of installable plugins across an ecosystem, every consumer plugin present in the baseline SHOULD hook the filter and drop its own slug — an active plugin should never advertise itself as an installable add-on. Any future AcrossAI plugin added to `AddonsPageRenderer::ADDONS` should ship the same filter with its own slug on activation.
- **Future mistake prevented**: Retiring a vendor integration by leaving the consumer-side instantiation block behind (relying only on `class_exists` to fall false) leaves dead code in `Main::define_admin_hooks()` that reviewers keep re-explaining. Delete the block outright and point INDEX/DECISIONS/BUGS to the retirement feature via `Superseded (F###)` so the historical body stays for context without pretending to be current guidance. Symmetrically, forgetting the consumer self-exclusion filter causes an active plugin to render its own card on the Add-ons page — visually confusing (Activate button on an already-active plugin) and a UX regression once main-menu's card render inspects `is_plugin_active()`.
- **Evidence**: `acrossai-co/main-menu` 0.0.22 `composer.json` drops `freemius/wordpress-sdk` + `AcrossAI_Addon\` PSR-4 namespace. `AddonsPageRenderer::get_addons()` runs `apply_filters('acrossai_addons', self::ADDONS)` — pure array filter, no license state. Plugin-side: `includes/Main.php` -94 lines (Freemius/AddonsPage block); `admin/Partials/AddonsFilter.php` +59 lines (singleton with `remove_self()`); `tests/phpunit/Admin/AddonsFilterTest.php` +50 lines (4 cases covering strip+reindex, no-op, non-array input, non-array entries).
- **Where to look**: `docs/planings-tasks/028-remove-freemius-and-filter-self.md` for the spec + verification recipe. `admin/Partials/AddonsFilter.php` for the canonical shape any sibling plugin should copy — swap the `OWN_SLUG` constant. `vendor/acrossai-co/main-menu/src/AddonsPageRenderer.php` for the vendor filter contract. Related retired: `DEC-ADDONS-PAGE-VENDOR-CTOR-BOOT`, `DEC-FREEMIUS-DOUBLE-OPTIN-GATES-ACCOUNT`, `B28` — all flipped to `Superseded (F028)`.

### 2026-07-14 - F025 Server tools registration hooks — hybrid DB-authoritative tool storage + Tools tab UI + companion filter + vendor filter callback

**Milestone**: DB-authoritative per-server tool storage (hybrid boolean columns + presence rows) + Tools tab UI with operator-removable protocol tools via `ConfirmDialog` + companion-plugin filter for database servers + vendor filter callback for the default server.

**Durable lessons captured**:
- `DEC-F025-HYBRID-TOOL-STORAGE-PROTOCOL-VS-CURATED` — boolean columns for fixed sets + presence rows for open-ended sets, unioned via single canonical composer (extends F017 ExposureResolver pattern).
- `DEC-F025-V2-VENDOR-SOURCE-CROSS-CHECK-CADENCE` + `DEC-F025-RUNTIME-EVIDENCE-OVER-STATIC-REVIEW` — extends F020's "v2 for close-in-substance" lesson; static hook analysis is insufficient for vendors whose `add_action` runs inside their own `__construct`.
- `DEC-F025-TASKS-REVIEW-PRESERVATION-INVARIANT-AND-COVERAGE-MATRIX` — subtractive-edit tasks list preserved invariants; task-reviews produce coverage matrices.
- `B29` — vendor hook-listener race caused by `add_action` inside `__construct`.

**Notable process observations**:
- Five distinct security-review passes: plan v1 (3 INFO), plan v2 (1 LOW + 2 INFO), tasks (3 INFO), architecture (0), staged (2 INFO) — all remediated or documented before merge.
- Runtime evidence 2026-07-14 disproved v2's SEC-025-v2-2 "hook-order safe" verdict — corrected via FR-018 (POST validation bypass) + `ToolPolicy::PROTOCOL_TOOL_METADATA` (GET catalog fallback). Reinforces `DEC-F025-RUNTIME-EVIDENCE-OVER-STATIC-REVIEW`.
- `/speckit-analyze` after runtime fixes surfaced 5 spec-hygiene drifts (C1, C2, U1, U2, A1) — all closed by adding FR-018 + acceptance-scenario clarifications + contract doc updates.

**Where to look next**

- `specs/025-server-tools-registration-hooks/` (full artifact set)
- `docs/security-reviews/2026-07-13-025-*` (plan v1 + v2) + `2026-07-14-025-*` (tasks + staged)

---

### 2026-07-13 - F022 shared Add-ons submenu + umbrella-product Freemius model — 4 upstream vendor releases (main-menu 0.0.15 → 0.0.18) delivered in one branch

- **Why durable**: F022 established the pattern of iterating on an owned vendor package (`acrossai-co/main-menu`) inside a single spec-kit feature — each iteration produced a small, tagged upstream release consumed by a `composer.json` bump in the same feature branch. Four releases (0.0.15 → 0.0.16 → 0.0.17 → 0.0.18) landed under Phases 4b/4c/4d/4e, each with concrete task IDs (T024-T028, T029-T034, T035-T036, T037-T042). Each release captured its evidence via the vendor commit SHA + tag name, cited in the corresponding tasks.md phase. Without this discipline, the "which upstream commit closed which task" trail would evaporate at session end — future maintainers investigating "why does mcp-manager depend on main-menu 0.0.18 specifically" would have no traceable answer. This pattern is now the canonical shape for every future spec-kit feature that iterates on an owned vendor package.
- **Future mistake prevented**: A future feature that needs to iterate an owned vendor package will fold the vendor edits into an unnamed "Phase 4b" (or worse: unwritten inline changes) without capturing the SHA/tag chain per task. When a later feature needs to bump the same vendor and asks "what was in each intermediate version", the ledger is empty. F022 shows the correct discipline — every intermediate vendor tag has its own numbered phase in tasks.md with the specific commit SHA cited inline. Also prevents the CRITICAL-severity duplicate-task-ID mistake we made in F022 (Phase 5 T030-T033 collided with Phase 4c T029-T034; Phase 6 T040-T046 collided with Phase 4e T037-T042) by making it clear that each folded phase gets its own T-block that doesn't overlap the initial planning ranges.
- **Evidence**: `specs/022-addons-page-registration/tasks.md` §Phase 4b through §Phase 4e each name a specific upstream commit (`a58dec9` for 0.0.15, `0fb50ea` for 0.0.16, `d467f83` for 0.0.17, `a6a35ff` for 0.0.18) and the plugin-side task that consumed it. `docs/planings-tasks/022-addons-page-registration.md` §Emergent Fixes captures the diagnostic that motivated each release. PR #26 at `acrossai-co/acrossai-mcp-manager` reflects the four bumps across 4 separate commits (`d8bb5d8`, `c0cafaa`, `11af3d3`, `ba27058`) with matching README.txt Unreleased narrative extensions.
- **Where to look**: Any future feature that iterates an owned vendor package MUST match this shape — number the vendor-bump phase (`Phase 4b`, `Phase 4c`, etc.), cite the upstream SHA + tag, and reserve non-overlapping T-block ranges (e.g. Phase 4b uses T024-T028, Phase 5 uses T050+; don't start Phase 5 at T030 like F022 initially did — the correction is documented in F022 Phase 7 T070). Also: `A16` in this INDEX (composer VCS repo entry) is a co-companion invariant when the vendor is `acrossai-co/*`.

### 2026-07-12 - F021 OAuth 2.1 governance wrap-up — third D13 application (constitution v1.1.0), governance-gate scope drift caught, branch protection enabled

- **Why durable**: F021 (OAuth 2.1 + PKCE authorization server + AI Connectors nested tabs) went from code-complete-but-ungoverned to governed in a single session. Three lessons generalize beyond this feature: (1) the AI Connectors tab card layout (recurring across F020 shuttle picker, F021 Phase 9 card shell, F024 nested tabs) hit D13's threshold — ≥2 features + forward-looking recurrence — and was formalized via a v1.0.0 → v1.1.0 constitution amendment adding a narrowly-scoped "Connector picker card layout" exception paragraph to Principle IV, retiring the prior soft-deviation-per-feature ceremony (see `DEC-TOOL-SHUTTLE-PICKER-OVER-DATAVIEWS` that was planned but rendered obsolete); (2) the T118c "no `$wpdb` in Controllers" gate scanned Controllers only and silently missed the F024 admin-Partial layer bypass at `AIConnectorsTab.php:273-326` — captured as [[B26]] governance-gate scope drift and remediated by adding T118d + moving DB access behind `AccessTokenRepository` + `ClientRepository` methods; (3) F024 landing inline with F021 Phase 9 iteration without concrete task IDs in the parent tasks.md broke reviewer traceability for a day — captured as [[D22]] inline-fold-in tracking.
- **Future mistake prevented**: (a) Next time the same category of soft deviation appears in a third feature, jump straight to the constitution amendment ceremony rather than logging a fourth DEC. (b) Every layering gate MUST scan every layer that participates in the layering rule; validate every new gate against a known violation before shipping. (c) Follow-up features that ship inline with a parent's phase iteration MUST fold into the parent tasks.md as concrete IDs (`T<X>-<seq>` pattern), never as "See F###" pointers.
- **Evidence**: Constitution amendment at `.specify/memory/constitution.md` v1.1.0 (2026-07-12); architecture review report of same date (V1 Principle IV — resolved by amendment; V2 boundary erosion — resolved by R2 refactor; V3 gate scope drift — resolved by T118d; V4 task-sync drift — resolved by F024 fold-in). Two new memory entries [[D22]] + [[B26]]; a third [[B27]] captures the GitHub Actions matrix-cell check-name brittleness observed while wiring branch protection.
- **Where to look**: `.specify/memory/constitution.md §Principle IV` for the exception paragraph shape; `bin/verify-f021-gates.sh` for T118d as the reference "layer-inclusive gate" template; `specs/021-oauth-2-1-implementation/tasks.md §Phase 10` for the fold-in task-ID pattern; `.github/workflows/` + `.github/CODEOWNERS` for the branch-protected repo layout (6 pinned non-matrix check names, matrix PHPUnit intentionally omitted per [[B27]]).

### 2026-07-09 - Per-server tool selection (F020) — two workflow lessons: defer ADR capture past UX pivots, and run security-review v2 for close-in-substance verification

- **Why durable**: F020 shipped a hand-rolled shuttle picker, presence-based BerlinDB storage, and runtime enforcement at priority 30 on `mcp_adapter_pre_tool_call` (see `DEC-F020-TOOL-ENFORCEMENT-PRIORITY`). Two workflow-level lessons emerged that generalize beyond this feature: (1) A planned ADR — `DEC-TOOL-EDIT-EXPLICIT-BATCH-COMMIT`, describing a Save/Cancel batch commit workflow — had to be **withdrawn** when the operator asked to remove Save/Cancel in favor of optimistic-per-toggle POST after implementation. Any UX-facing ADR drafted before the operator has interacted with the shipped surface risks capturing wording that only stabilizes post-implementation. (2) A subtle "close-in-form, gap-in-substance" security regression (SEC-020-007, captured as `B24`) survived plan-review v1's remediation because v1's contract text was drafted from casual documentation rather than the vendor's actual source. A second plan-review pass (v2) after remediation caught it. For security-critical closure verification, a second review pass is worth the token cost.
- **Future mistake prevented**: (1) Future features MUST defer UX-facing ADR capture until AFTER `/speckit-security-review-staged` runs cleanly, not draft them speculatively in the plan phase. ADR bodies drafted pre-implementation may need withdrawal + rewrite if the operator's shipped-surface interaction diverges from the pre-planned UX. (2) Any feature that involves cross-package integration (WordPress action/filter payloads carrying vendor objects) MUST verify contract text against the actual vendor source — copying accessor patterns from casual documentation is a durable source of silent-failure bugs (captured as `B24`).
- **Evidence**: `docs/security-reviews/2026-07-09-020-per-server-tool-selection-plan.md` (v1 findings) → `docs/security-reviews/2026-07-09-020-per-server-tool-selection-plan-v2.md` (v2 caught SEC-020-007) → `docs/security-reviews/2026-07-09-020-per-server-tool-selection-tasks.md` → `docs/security-reviews/2026-07-09-020-per-server-tool-selection-staged.md` (INFORMATIONAL only). Post-implementation pivots documented in `specs/020-per-server-tool-selection/tasks.md §Post-implementation UX pivots`. Withdrawn ADR notation lives in `specs/020-per-server-tool-selection/spec.md §Assumptions §Optimistic-per-toggle POST`.
- **Where to look**: `contracts/enforcement.md` (F020 canonical gate contract, priority 30). `Includes\MCP\ToolExposureGate::gate_tool_call_by_curation()` for the shipped duck-typed enforcement pattern. `Includes\Database\MCPServerTool\Query::replace_set()` for the transactional Save with `SELECT ... FOR UPDATE`. Canonical decisions: `DEC-TOOL-SELECTION-PRESENCE-MODEL` (storage model) + `DEC-F020-TOOL-ENFORCEMENT-PRIORITY` (gate stack slot).

### 2026-07-07 - Per-server ability selection (F017) — storage-vs-enforcement decoupling caught mid-flow by the plan-review security gate

- **Why durable**: F017 shipped a BerlinDB table storing per-`(server, ability)` exposure overrides, an interactive `@wordpress/dataviews` React tab, extensibility filters — and initially, **nothing that made those overrides bite at the MCP tool-call boundary**. The plan-phase `/speckit-security-review-plan` surfaced this as SEC-001 HIGH (CWE-863 Incorrect Authorization). Storage-vs-enforcement decoupling is a durable failure mode: whenever a feature adds admin storage that MUST also be consulted by a runtime enforcement site, the wiring is a separate design decision that the storage-side task list will silently omit. Codifying "always trace stored decisions back to their enforcement site" prevents the class.
- **Future mistake prevented**: A future feature that adds new admin-writable state (per-user quotas, per-endpoint rate limits, per-thing X) will silently ship UI + REST + tests — but no enforcement — unless the plan phase explicitly names the enforcement site and adds a task for wiring the storage read into it. The plan-review security phase catches this, but only if the reviewer looks for the enforcement gap. Making the check a durable checklist item catches it earlier.
- **Evidence**: SEC-001 finding in `docs/security-reviews/2026-07-07-017-per-server-ability-selection-plan.md`. Resolved via `/speckit-clarify` Q4 → FR-030 → TASK-10 in the same session — added `Includes\MCP\AbilityExposureGate` wired on `mcp_adapter_pre_tool_call` at priority 20 (F015 = 10), so F017's hidden verdict runs later and supersedes any F015 "allow." Never overrides an F015 deny.
- **Where to look**: `Includes\MCP\AbilityExposureGate::gate_tool_call_by_exposure()` for the enforcement-side pattern. `Includes\REST\AbilitiesController::build_row()` for the storage-side READ. `Includes\Database\MCPServerAbility\ExposureResolver` is the shared truth. `DEC-ABILITY-OVERRIDE-RESOLUTION` codifies the resolver contract; every future storage-writes-plus-enforcement-reads pattern MUST route through a single resolver — do not duplicate the fallback rule.

### 2026-07-07 - Fresh-install-only retirement pattern (F016 Claude Connectors)

- **Why durable**: Retirement features default to shipping self-healing schema migration code (idempotent `DROP TABLE` + `ALTER TABLE` in Activator, `column_exists`-gated fallback helpers, `$version` bumps triggering BerlinDB diff runs). F016's initial plan followed that pattern. The 2026-07-07 operator-directive scope reduction ("we are not looking for backward compatibility") eliminated ALL in-plugin migration code and delegated it to a manual SQL recipe published in README.txt §Unreleased. Net effect: smaller diff (~80 fewer LOC of migration helpers), zero test surface for migration edge cases, no Activator risk of running destructive SQL by mistake, no coordinated-deployment `$version` bump. This is the canonical shape for future retirements when operator attestation covers no-live-data OR the operator explicitly manages migration.
- **Future mistake prevented**: A future retirement feature that reflexively adds self-healing migration to Activator will ship more code, more test surface, more moving parts, and more Activator-destructive-SQL risk than needed. The reflex is wrong when the operator has explicit control over deployment. Verify attestation FIRST; if attestation covers no-live-data, apply this pattern.
- **Evidence**: Initial plan (`specs/016-remove-claude-connectors/plan.md` pre-2026-07-07 revision) included `ConnectorColumnMigration.php` optional helper, `DROP TABLE IF EXISTS` block in Activator, `$version 0.0.1 → 0.0.2` bump. All removed 2026-07-07 per user directive. Post-implementation grep audit (T031): 0 hits from baseline 370. Security review confirmed the retirement retroactively closed an S3 constitution gap (plaintext `claude_connector_client_secret` in `varchar(255)`) without shipping migration code — the operator's `UPDATE ... SET ''` step in the README recipe forces InnoDB page rewrite before column drop (SEC-016-001 defense-in-depth).
- **Where to look**: Pattern reference: `specs/016-remove-claude-connectors/research.md` Decision 1 + Decision 5. Operator recipe: `README.txt` §Unreleased. Contrast: F011 (2026-07-02) which SHIPPED the phantom-version guard because that migration WAS a runtime operation, not an operator recipe. Canonical decision: `DECISIONS.md::D21`.

### 2026-07-04 - Post-implementation drift audit: run /speckit-analyze AFTER /speckit-implement, not only before

- **Why durable**: When mid-implementation clarifications supersede the initial spec (F015's Q4 superseded Q1 SAFE_CAPABILITIES; user directed a mid-session `TABLE_SLUG` rename `mcp_manager` → `mcp`; user directed a pivot from hand-rolled PHP form to vendor React `<AccessControl>` component; T030 dead-code follow-up), the shipped code drifts from spec + tasks + memory-synthesis + security-constraints + architecture-violations in ways the pre-implement `/speckit-analyze` cannot catch. Running `/speckit-analyze` AFTER `/speckit-implement` (as a spec-hygiene checkpoint) catches: stale FR text still describing withdrawn designs, table-name/slug references that missed the rename sweep, dead-code follow-up tasks not yet added, historical clarification records that need preservation vs. amendment, docs-vs-shipping drift in per-feature `security-constraints.md` and `architecture-violations.md`.
- **Future mistake prevented**: A future feature that ships with mid-session clarifications supersession or user-directed pivots will have its spec docs drift out of sync with the shipped code — reviewers reading the archive later will believe the FR text (e.g. "form emits `<form method="post">`") when the code ships something completely different (a vendor React mount div). Running the post-implement audit catches this before the docs get committed, preserving the archive's trustworthiness.
- **Evidence**: F015 post-implement `/speckit-analyze` (this session) surfaced 4 CRITICAL/HIGH drift categories — F1 table slug (~55 stale `mcp_manager` occurrences across 6 spec files), F2 vendor React pivot (FR-009/FR-010/T011/T012 all still described the withdrawn PHP form), F3 SAFE_CAPABILITIES cascade (FR-025 marked SUPERSEDED but 5 downstream references still active-context), F13 uninstall wording gap. All 4 remediated in the same session; second-pass `/speckit-analyze` returned zero CRITICAL/HIGH findings.
- **Where to look**: `.claude/skills/speckit-analyze/` for the audit prompt; the F015 spec + tasks + memory-synthesis + security-constraints + architecture-violations files as the canonical example of "what drift looks like when it's caught and fixed" (see the "amended 2026-07-04 —" markers throughout). For workflow: add `/speckit-analyze` after `/speckit-architecture-guard-governed-implement` in any feature that had Clarifications sessions or user-directed pivots.

### 2026-07-03 - Vendor-owned shared Settings page: register_setting() option_group MUST be the shared slug, not the per-tab page slug

- **Why durable**: The vendor-owned `PageRenderer::render()` emits ONE `settings_fields('acrossai-settings')` nonce for the whole shared page — every consumer plugin's `register_setting()` MUST target that shared option group or Save silently no-ops with no operator-visible error. Getting this wrong looks like a working form (nonce validates, `options.php` accepts the POST) until the reload shows the toggle reverted to its prior value. Feature 012 is the first cross-plugin consumer to formalize this contract; codifying it here prevents every subsequent consumer plugin from rediscovering the trap.
- **Future mistake prevented**: A future AcrossAI plugin developer using the standard WordPress Settings API `register_setting( '<page-slug>', ... )` reflex — which is correct for a plugin-owned single-page Settings screen — silently breaks Save on the shared vendor page. Reviewers looking at just the tab code cannot spot the bug without knowing the vendor contract; the failure is invisible in phpstan, phpcs, and even PHPUnit runs that stub the option layer.
- **Evidence**: Feature 012 spec.md CONSTRAINTS ("Do not change the 'acrossai-settings' option group"); Feature 012 security review SEC-012 Trust Boundaries table ("Vendor Save routing → wrong option group"); Sibling `acrossai-abilities-manager/admin/Partials/SettingsMenu.php:111-118` — matching pattern; DEC-VENDOR-SETTINGS-TAB-INTEGRATION rule 3.
- **Where to look**: `admin/Partials/SettingsMenu.php::register_settings()` for the canonical shape. Any future AcrossAI plugin that adds a tab MUST match this exact shape verbatim — the `option_group` argument is the load-bearing invariant, not the `page_slug`.

### 2026-07-02 - BerlinDB Table subclasses must override maybe_upgrade() with a phantom-version guard

- **Why durable**: The phantom-version guard on every BerlinDB-backed Table subclass is a canonical safety belt against the "version option stamped but physical table missing" edge case. Costs one method override; prevents an entire class of hard-to-diagnose "table doesn't exist" activation bugs.
- **Future mistake prevented**: A future BerlinDB-backed table shipped without the guard could silently short-circuit `maybe_upgrade()` on any install where a prior activation failed mid-DDL. The bug is invisible until users complain about missing rows/features.
- **Evidence**: The observed `wp_acrossai_mcp_servers doesn't exist` symptom that originally motivated Feature 011 on the developer's local install (2026-07-02).
- **Where to look**: The four subclass file paths — `includes/Database/{MCPServer,CliAuthLog,OAuthToken,OAuthAudit}/Table.php` — each contains a `public function maybe_upgrade(): void` override. Canonical sibling reference at `/Users/raftaar1191/local-sites/wordpress-7-0/app/public/wp-content/plugins/acrossai-abilities-manager/includes/Modules/Abilities/Database/AcrossAI_Abilities_Table.php:96-101`.

## 2026-07-21 — F032 OAuth Per-Server Scoping

**Milestone**: shipped the P1 security fix closing a critical cross-server privilege-escalation gap on the AI Connectors admin surface.

**Why durable**: first application of the D28 3-part contract to THREE coordinated tables (`oauth_clients`, `oauth_tokens`, `oauth_auth_codes`), with cross-Table observability aggregation coordinated via registration order in `Main::reconcile_database_schemas()`. Established the "first-class tenant column, never composite-string prefix" invariant (`D31` + `B37`) that generalizes to any future multi-tenant admin surface. Codified two new observability signals (`acrossai_mcp_oauth_cross_server_attempted`, `acrossai_mcp_oauth_legacy_dcr_purged`) both fired with 4-arg signatures that intentionally do NOT include the actual owning tenant id — preserving the "response body doesn't leak cross-server existence" invariant on the signal path too.

**Future mistake prevented**: any future PR adding a per-tenant admin REST endpoint that accepts only a tenant-scoped identifier without matching tenant validation. Grep gate in `B37`.

**Evidence**: `specs/032-oauth-per-server-scoping/{spec,plan,tasks,research,data-model,quickstart}.md` + `specs/032-oauth-per-server-scoping/contracts/*.md` (4 REST contracts) + `docs/security-reviews/2026-07-21-032-oauth-per-server-scoping-{plan,plan-v2,tasks}.md` + `docs/planings-tasks/032-oauth-per-server-scoping.md` (planning brief in 011-berlindb-migration shape).

**Where to look**: `D31` (canonical decision), `B37` (grep gate for future features), planning-doc `TASK-9 §Memory hygiene` for the templates used.

## 2026-07-22 — F032 extended scope (folded into original F032 spec-kit)

**Milestone**: after the original F032 core landed, an additional 22 FRs (FR-029..FR-050) + 9 SCs (SC-013..SC-021) + 4 new user stories (US5-US8) were built inline on the same branch and folded back into the F032 spec/plan/tasks via `/speckit-analyze` → in-place spec expansion. Rationale for one-spec fold-in vs. splitting into F033/F034/F035: all touch the same call sites (ConnectorAdminController + AIConnectorsTab + Main.php hook wiring); splitting would fragment a single audit surface. Alternative (splitting) evaluated and rejected — see analyze report.

**What was added**:
- **NEW BerlinDB module** `wp_acrossai_mcp_connector_approved_users` (US5) — 4 files, 6 columns, 4 indexes, F017 MCPServerAbility shape, F011 phantom-version guard, DEC-BERLINDB-SUBCLASS-NO-USE-COLLISION leading-\ FQN. Fresh-install-only per D21.
- **NEW admin panel** "Approved Users" between Connections and Settings when `require_admin_approval = true` (US5) — level-3 nav conditional render + `render_approved_users_panel()` with Pending + Approved sections.
- **NEW hook contract** `acrossai_mcp_connector_user_approval_revoked` + opt-out filter `acrossai_mcp_connector_revoke_tokens_on_approval_revoked` (US6) — default listener cascades approval-revoke → token-revoke; codified as D32.
- **NEW nuclear endpoint** `POST /oauth/revoke-client-tokens-all-servers` (US7) — deliberate D31 carve-out with mutually-exclusive observability action (`acrossai_mcp_oauth_client_revoked_across_all_servers` NEVER co-fires with `acrossai_mcp_oauth_cross_server_attempted`); codified as D34.
- **NEW annotated token display** `"2 (1 access · 1 refresh)"` in Connections panel (US7) — replaces opaque totals so operators can distinguish access vs refresh at a glance.
- **NEW AC connection-time gate** at OAuth authorize / CLI device-grant / Application Password (US8) — shared `user_has_server_access()` helper, fail-open per D19, fires `acrossai_mcp_access_control_denied` with distinct `$context` per surface; codified as D33.
- **NEW cascade** — UserLifecycle::on_user_deleted extended with `ConnectorApprovedUsersQuery::delete_by_user_id()` (FR-037 / FR-042 extension).
- **NEW REST endpoints** `POST /oauth/deny-pending-consent` + `POST /oauth/revoke-user-approval` (US6, both under shared `admin_permission` = manage_options + wp_rest nonce).
- **NEW enriched 403** at F015 AC tool-call boundary — includes `server_slug` + `user_roles` (FR-050).
- **NEW cascade DB helpers** `TokensQuery::{revoke_by_user_and_server_and_client_ids, revoke_by_client_id_and_user_id, revoke_by_client_id_across_all_servers, count_active_by_client_id_and_server_id_grouped}` — every raw query uses `$wpdb->prepare()` with `0`/`0`/`` (per-client loop avoided dynamic `IN()` PHPCS false-positive).

**Testing status**: Implementation shipped on-branch (PHPCS clean 12/12, PHPStan L8 clean); tests outstanding per audit gaps C1/C2/C3 → new tasks T079-T080 + T087-T088 + T094-T095 + T099 in tasks.md Phase 8-11. Deliberate deviation from tests-first for the folded-in scope — MUST close testing gaps before merge per §II.

**Why durable**: proves the "in-branch scope expansion + `/speckit-analyze` gap detection + in-place spec fold-in" workflow. Alternative (fragmenting into F033/F034/F035 mid-branch) would have created 3× the audit surface with the same code delivered. Fold-in is safe here because all changes touch the same audit surface + share the same security review chain.

**Where to look**: `D32 / DEC-CONNECTOR-APPROVAL-REVOKE-CASCADE`, `D33 / DEC-OAUTH-AUTHORIZE-AC-GATE`, `D34 / DEC-CROSS-SERVER-NUCLEAR-REVOKE-CARVE-OUT`, `specs/032-oauth-per-server-scoping/{spec,plan,tasks}.md` §"F032 Extended Scope" sections, `specs/032-oauth-per-server-scoping/tasks.md` §Phase 8-11 (T068-T104).

## 2026-07-22 — F032 SEC-L1 remediation (staged-review follow-up)

**Milestone**: applied SEC-L1 remediation from the F032 staged security review — added distinct `acrossai_mcp_connector_admin_self_bypassed` observability action to the FR-051 admin-bypass branch so forensic reviewers can differentiate self-service bypass rows from explicit-reviewer approval rows in `wp_acrossai_mcp_connector_approved_users`.

**Why durable**: codifies `B38 / B-ADMIN-SELF-APPROVAL-AUDIT-TRAIL-AMBIGUITY` as a durable pattern. Any future admin bypass path that auto-inserts into a reviewer-approved table MUST ship either a schema-level discriminator OR a distinct observability action — never both semantics into the same signal.

**What was added**:
- **NEW action** `acrossai_mcp_connector_admin_self_bypassed( int $server_id, string $connector_slug, int $user_id, int $timestamp )` — 4-arg fire-and-forget observability from FR-051 bypass branch. No filter opt-out (pure observability, not enforcement). Documented in `contracts/php-hooks.md` + spec.md FR-051 + SC-023.
- **NEW test coverage** in `tests/phpunit/OAuth/AdminBypassTest.php` — 2 new methods verifying (a) admin bypass fires the action exactly once with 4 correct args, (b) subscriber path MUST NOT fire the action.
- **NEW durable memory entry** `B38 / B-ADMIN-SELF-APPROVAL-AUDIT-TRAIL-AMBIGUITY` in `docs/memory/BUGS.md` — grep-gate + prevention pattern for future admin bypasses.

**Chosen approach**: application-level (distinct action) over schema-level (`bypass_reason` column) — smaller diff, no D28 upgrade needed, matches the plugin's existing observability pattern.

**Where to look**: `B38 / B-ADMIN-SELF-APPROVAL-AUDIT-TRAIL-AMBIGUITY`, `specs/032-oauth-per-server-scoping/spec.md` FR-051 + SC-023, `specs/032-oauth-per-server-scoping/contracts/php-hooks.md`, `docs/security-reviews/2026-07-22-032-oauth-per-server-scoping-staged.md`.

---

## 2026-07-29 — F038 User-accessible MCP servers shortcode + reusable base class

**Frame**: F038 ships `[acrossai_mcp_servers]` — a frontend shortcode that lists every MCP server the current logged-in user can access (F015 gate) whose F037 Embeds tab has the master toggle ON and at least one enabled connection method. Ships alongside a data-only abstract base class `\AcrossAI_MCP_Manager\Public\Renderers\UserServers\AbstractUserServersRenderer` under `public/Renderers/UserServers/` — the extension surface for the planned BuddyBoss add-on, WooCommerce My Account extension, WPUM, MemberPress, etc.

**Novelty**: Pure composition on top of F011 MCPServerQuery + F015 access-control wrapper + F035 ConnectionMethodRegistry + F037 AbstractEmbedTransport. **Zero** new DB tables, REST endpoints, admin surfaces, or JS. Two new filters (`acrossai_mcp_user_accessible_servers`, `acrossai_mcp_servers_shortcode_html`). One new shortcode. Total production surface: 2 PHP files (~460 LOC combined) + one insertion in `Main.php`. `AbstractUserServersRenderer` is `abstract` under `public/Renderers/` — first case of a subclass extension surface at that path (precedent `AbstractClientRenderer` established the shape).

**Governance patterns established**:

- **D40 / DEC-USER-SCOPED-ENUMERATION-COMPOSES-GATES** — durable rule for future "list what current user can reach" primitives. Compose the shipping gate stack (`user_has_server_access` + `is_enabled_for_server`) — never inline the underlying meta reads. Preserves fail-open + memoization + admin-bypass. Enforced via three grep-gates at review time.
- **D36 precedent-based deviation logged** — `AbstractUserServersRenderer` is `abstract` under `public/` despite D36's `final class` guidance for `@experimental public`. Reasoning: the base IS the extension surface; `final abstract` is a language contradiction; D36's target (delegation-invariant defeat) does NOT apply because F038 IS the gate-application layer, not a gate-enforcer other features must hit. Documented in `plan.md` §Complexity Tracking.
- **SEC-001 caller-authority responsibility** — companion plugins passing an arbitrary `$target_user_id` MUST independently verify the current viewer's authority. Documented in class docblock + `AbstractUserServersRenderer.contract.md`. Prevents cross-user access-control-policy enumeration if a companion plugin ships without a caller-side guard.

**Quality gates (all green pre-merge)**:

- PHPCS strict: 0 errors / 0 warnings on `public/Renderers/UserServers/` + `tests/phpunit/Public/Renderers/UserServers/` + touched lines in `Main.php`.
- PHPStan level 8: 0 errors on all F038 files.
- npm validate-packages: green.
- All 5 grep-gates from spec DoD: pass (no re-firing `acrossai_mcp_embed_transports` / `acrossai_mcp_client_classes`; no direct `_embeds_enabled` / `_embeds_clients` meta reads; no `public/` → `includes/` back-import; exactly one `add_shortcode()` in the subsystem).

**Security reviews**:

- `/speckit-security-review-plan` — LOW (0 CRITICAL / 0 HIGH / 0 MODERATE / 1 LOW SEC-001 / 4 INFO SEC-002/003/004/005). All findings applied as contract + code documentation.
- `/speckit-security-review-tasks` — LOW (1 LOW SEC-T-001 remediation applied to T007+T008 tasks; 2 INFO advisories).
- `/speckit-architecture-guard-violation-detection` — 0 violations across A–E.

**Test suite**: new `user-servers` PHPUnit suite registered in `phpunit.xml.dist` + CI job in `.github/workflows/phpunit.yml`. Three test files — `AbstractUserServersRendererTest.php` (14 tests including T020 US3 payload-filter round-trip), `UserServersBlockTest.php` (16 tests including T021 US3 HTML-filter wrapping + XSS at-boundary regression), `ThirdPartyExtensibilityTest.php` (3 tests including SC-005 fake-fourth-transport proof).

**Where to look**:

- Brief: `docs/planings-tasks/038-user-accessible-mcp-servers-shortcode.md`
- Spec-kit spec dir: `specs/037-user-accessible-mcp-servers-shortcode/` (naming offset per F037-established convention)
- Production: `public/Renderers/UserServers/{AbstractUserServersRenderer,UserServersBlock}.php`
- Tests: `tests/phpunit/Public/Renderers/UserServers/*Test.php`
- Wiring: `includes/Main.php::define_public_hooks()` (single-line insertion alongside F037 EmbedBlockRenderer wiring)
- Memory: `D40 / DEC-USER-SCOPED-ENUMERATION-COMPOSES-GATES` in `docs/memory/DECISIONS.md`
- Security reviews: `docs/security-reviews/2026-07-29-037-user-accessible-mcp-servers-shortcode-{plan,tasks}.md`

**Follow-up (not blocking)**: `/speckit-security-review-branch` + `/speckit-architecture-guard-architecture-review` post-implement scans; `/speckit-analyze` cross-artifact consistency check; then `/speckit-git-commit` → PR against `main`.

### 2026-08-24 — Local-dev TLS bypass affordance for MCP client JSON snippets (F075)

- **Why durable**: Names the "silent connected-but-no-tools" symptom that stumps every operator on their first local-dev try — retired as `BUGS.md` "MCP client connected but tool list empty on local dev" the same day the affordance shipped. Every current and future MCPClient subclass (built-in or contributed via `acrossai_mcp_client_classes` filter) inherits the affordance for free via `AbstractMCPClient::build_env()`.
- **Future mistake prevented**: A new MCPClient contributor won't re-inline the pre-F075 env-array literal (which would drift from the shared helper and skip the TLS bypass on local dev). Also anchors the broader principle "dev-only affordances go through `Utilities/*Environment` detection helpers, not admin toggles" — the affordance's blast radius is bounded by the static detection rule, not the operator's discipline.
- **Evidence**: F075 refactored 16 concrete client classes, added `Utilities\LocalEnvironment` (mirrors `SiteSlug` shape), 100 / 100 PHPUnit assertions green, PHPCS + PHPStan L8 clean on all touched files. Live sites unaffected (verified via production simulation per spec § SC-003).
- **Where future contributors should look**:
    - `includes/Utilities/LocalEnvironment.php` — detection rule + `troubleshooting_doc_url()` constant.
    - `includes/MCPClients/AbstractMCPClient::build_env()` — injection point.
    - `docs/memory/DECISIONS.md` — `DEC-MCPCLIENT-BUILD-ENV-SHARED` + `DEC-LOCAL-DEV-AFFORDANCE-SCHEME-AGNOSTIC` + (follow-up) `DEC-ABSTRACT-BASE-DEFAULT-COMPOSES-FROM-SIBLING-METHODS`.
    - `docs/planings-tasks/075-local-dev-tls-bypass-notice.md` + `specs/075-local-dev-tls-bypass-notice/` — design trace.
    - `tests/phpunit/MCPClients/LocalEnvironmentTest.php` — the eleven detection axes.

- **Follow-up polish (same day, commits 9f98420 / b4ff6ad / 49661d1)**:
    - **Deep-link fix (B50)**: Wizard's Step 11 was rendering the client metadata DTO instead of the mcpServers config JSON when the operator deep-linked via `?server=1` in the admin URL. Root cause was dual — backend's `handle_state()` only attached the `config` field when the scratchpad had `server_id > 0`, AND the frontend's XHR to `/state` didn't forward the admin URL's `?server` param. Fixed both ends: frontend forwards as `?server_id=N`; backend accepts it as a scratchpad-empty fallback. Second-order lesson (baked into B50): React components should NOT fall back to `JSON.stringify(dto)` on missing fields — the raw dump leaks internal shape.
    - **Step 5 restart affordance (D47)**: Both surfaces (wizard Step 11 + per-server MCP Clients admin tab) now surface a client-specific restart / reload instruction after the "Copy Configuration" button. New method `AbstractMCPClient::get_restart_step_text()` with a composed default (`sprintf("Restart %s to load the new MCP server.", $this->get_client_name())`) so third-party subclasses ship a working Step 5 without needing to override. Six overrides for clients whose action isn't a plain restart (VS Code reload, GitHub Copilot dual-target, Cline / Roo Code / Kilo Code hot-reload, Custom Client generic). Codified as `DEC-ABSTRACT-BASE-DEFAULT-COMPOSES-FROM-SIBLING-METHODS` — every future per-subclass display-string method must compose from a sibling abstract, not hardcode a generic string.
    - **Cross-surface parity via D43**: The restart affordance ships on both the wizard AND the per-server admin tab from the same source-of-truth method — D43 (cross-surface DTO producer unification) applied for the second time. Any future per-client display property (color code, sidebar hint, keyboard shortcut, etc.) should follow the same shape: add a method on `AbstractMCPClient`, surface it on the `ConnectionMethodRegistry` DTO for the wizard, call it directly from `MCPClientsBlock` for the admin tab.
    - **UX copy simplification**: Local-dev notice shortened from three sentences to a single line ending in "More info" — the doc link carries the detail, the notice just marks the flag's presence. Single source of truth in PHP reached from JSX via `wp_localize_script.tlsBypass.{message,linkText,docUrl}`.

### 2026-08-24 — Client picker emojis retired from both admin surfaces (F076)

- **Why durable**: Anchors the "subtractive UI change ≠ subtractive extension surface" pattern via `DEC-RETIRE-UI-USAGE-KEEP-EXTENSION-SURFACE`. Future maintainers picking up F076's touched files (`MCPClientsBlock.php`, `Step11_ClientDetail.jsx`) will see an "unused" `$client->get_icon()` extension surface remaining on the DTO and on 16 client subclasses; the DEC explains why deleting them would be a silent third-party break, not a cleanup.
- **Future mistake prevented**: A subsequent maintainer won't delete `AbstractMCPClient::get_icon()` or the DTO's `'icon'` field on the reasoning "the pickers no longer render it." Companion plugins registering their own MCP client subclass via `acrossai_mcp_client_classes` continue to receive the same DTO shape.
- **Evidence**: F076 shipped 4 canary greps (SC-003..006). Three of them (SC-004, SC-005, SC-006) prove FR-005 preservation of the extension surface. Also codified `B51 / B-RETIRED-SYMBOL-NAME-IN-COMMENT-TRIGGERS-CANARY-GREP` after the SC-003 grep initially reported a false positive on a code comment that referenced the retired symbol — a noise-only but real gotcha for future subtractive-change PRs.
- **Where future contributors should look**:
    - `docs/memory/DECISIONS.md` — `DEC-RETIRE-UI-USAGE-KEEP-EXTENSION-SURFACE`.
    - `docs/memory/BUGS.md` — the 2026-08-24 "Retired-symbol names inside code comments" entry (F076 authoring gotcha).
    - `specs/076-remove-client-picker-icons/` — full design + verification trace.
    - `docs/planings-tasks/076-remove-client-picker-icons.md` — plain-English planning doc.
